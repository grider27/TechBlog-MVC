    {{#each posts as |post| }}
    <div class="row align-center mb-5">
        <div class="col-md-2">
            <p>{{post.title}}
            <p>
        </div>
        <div class="col-md-6">
            <p>{{post.content}}</p>
        </div>
        <div class="col-md-2">
            <p>{{user.name}}</p>
        </div>
    </div>
    {{/each}}